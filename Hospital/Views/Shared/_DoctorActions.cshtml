@model int

<li>
    <a href="@Url.Action("Appointments", "Doctors", new { id = "" })">
        Appointments
        @if (Model > 0)
        {
            <span class="badge">@Model</span>
        }
    </a>
</li>
